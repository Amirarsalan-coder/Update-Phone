let charge = 20;
let locait = "Dubai";

if (
  (locait === "vancover" || locait === "Dubai" || locait === "California") &&
  charge < 80 &&
  charge > 15
) {
  console.log("Your device is ready to update");
} else {
  console.log("Your can not to update");
}
